export default function HomePage() {
    return (
        <main>
            <h1>Bienvenue sur Open Family</h1>
            <p>
                Application React + Symfony API
            </p>
        </main>
    );
}